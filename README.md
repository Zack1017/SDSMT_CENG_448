# SDSMT_CENG_448

### Using VS Code

Complete the instruction on D2L 

1. Open the .vscode file and edit the launch.json file 

    a. Remove line 18 

    `    "runToMain": true,"`

    b. Adjust the last line to 

    `    "executable": "${workspaceRoot}/bin/${workspaceFolderBasename}.elf" `

3. Add the CMake Extention 
4. Add the CMake Tools Extention 
5. Close VS Code
6. In terminal navigate to code directory 
7. One just the lab in vs code using 
`Code Lab_#`
8. Go to CMake tools in the left menue
9. Select build 
    a. Select GCC 13.2.1 arm-non-eabi
10.  You should be all set 


Note: To open a lab you must use 
`Code Lab_#`
in order to ensure correct operaton 